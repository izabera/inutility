#include "lib/common.h"

int main(int argc, char *argv[]) {
  options("deua:b:l:n:t:", .arglessthan = 3);
  if (flag('b'))
  FILE *fileptr = stdin;
  size_t len;
  ssize_t read;
  while (read = get
  return errno;
}
